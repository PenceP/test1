<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- Fullscreen Backdrop -->
    <ImageView
        android:id="@+id/hero_backdrop"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop" />

    <!-- Ambient Mesh Gradient Overlay (updated dynamically) -->
    <View
        android:id="@+id/ambient_background_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Gradient Overlay -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/fullscreen_gradient" />

    <!-- Navigation Dock -->
    <FrameLayout
        android:id="@+id/navigation_dock"
        android:layout_width="72dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:layout_marginTop="78dp"
        android:layout_marginBottom="78dp"
        android:translationX="-28dp"
        android:layout_gravity="start|center_vertical"
        android:clipChildren="false"
        android:clipToPadding="false">

        <View
            android:id="@+id/navigation_dock_background"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/nav_glass_background"
            android:elevation="6dp" />

        <LinearLayout
            android:id="@+id/navigation_sidebar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center_horizontal"
            android:orientation="vertical"
            android:paddingStart="34dp"
            android:paddingEnd="12dp"
            android:paddingTop="32dp"
            android:paddingBottom="32dp"
            android:clipChildren="false"
            android:clipToPadding="false">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/nav_search"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_marginBottom="18dp"
                android:background="@drawable/nav_icon_button_background"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:insetBottom="0dp"
                android:insetTop="0dp"
                android:nextFocusRight="@id/content_rows"
                app:cornerRadius="32dp"
                app:icon="@drawable/ic_search"
                app:iconGravity="textStart"
                app:iconPadding="0dp"
                app:iconSize="32dp"
                app:iconTint="@color/nav_icon_color"
                app:rippleColor="@color/nav_button_ripple" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/nav_home"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_marginBottom="18dp"
                android:background="@drawable/nav_icon_button_background"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:insetBottom="0dp"
                android:insetTop="0dp"
                android:nextFocusRight="@id/content_rows"
                app:cornerRadius="32dp"
                app:icon="@drawable/ic_home"
                app:iconGravity="textStart"
                app:iconPadding="0dp"
                app:iconSize="32dp"
                app:iconTint="@color/nav_icon_color"
                app:rippleColor="@color/nav_button_ripple" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/nav_movies"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_marginBottom="18dp"
                android:background="@drawable/nav_icon_button_background"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:insetBottom="0dp"
                android:insetTop="0dp"
                android:nextFocusRight="@id/content_rows"
                app:cornerRadius="32dp"
                app:icon="@drawable/ic_movie"
                app:iconGravity="textStart"
                app:iconPadding="0dp"
                app:iconSize="32dp"
                app:iconTint="@color/nav_icon_color"
                app:rippleColor="@color/nav_button_ripple" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/nav_tv_shows"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_marginBottom="18dp"
                android:background="@drawable/nav_icon_button_background"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:insetBottom="0dp"
                android:insetTop="0dp"
                android:nextFocusRight="@id/content_rows"
                app:cornerRadius="32dp"
                app:icon="@drawable/ic_tv"
                app:iconGravity="textStart"
                app:iconPadding="0dp"
                app:iconSize="32dp"
                app:iconTint="@color/nav_icon_color"
                app:rippleColor="@color/nav_button_ripple" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/nav_settings"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:background="@drawable/nav_icon_button_background"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:insetBottom="0dp"
                android:insetTop="0dp"
                android:nextFocusRight="@id/content_rows"
                app:cornerRadius="32dp"
                app:icon="@drawable/ic_settings"
                app:iconGravity="textStart"
                app:iconPadding="0dp"
                app:iconSize="32dp"
                app:iconTint="@color/nav_icon_color"
                app:rippleColor="@color/nav_button_ripple" />
        </LinearLayout>
    </FrameLayout>

    <!-- Main Content Area -->
    <LinearLayout
        android:id="@+id/movies_content_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginStart="80dp"
        android:layout_marginTop="@dimen/layout_margin_top_hero_container"
        android:layout_marginBottom="0dp"
        android:clipChildren="false"
        android:orientation="vertical"
        android:weightSum="2.0">

        <!-- Hero Info Section -->
        <LinearLayout
            android:id="@+id/hero_info_container"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="0.8"
            android:gravity="top"
            android:orientation="vertical"
            android:paddingStart="@dimen/hero_container_padding_start"
            android:paddingEnd="@dimen/hero_container_padding_end"
            android:layout_marginStart="0dp"
            android:paddingBottom="@dimen/hero_container_padding_bottom">

            <ImageView
                android:id="@+id/hero_logo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/hero_logo_margin_top"
                android:layout_marginBottom="@dimen/hero_logo_margin_bottom"
                android:adjustViewBounds="true"
                android:maxWidth="@dimen/hero_logo_max_width"
                android:maxHeight="@dimen/hero_logo_max_height"
                android:scaleType="fitCenter"
                android:visibility="gone"
                android:contentDescription="@string/hero_logo_content_description"
                tools:visibility="visible"
                tools:src="@drawable/ic_movie" />

            <!-- Title -->
            <TextView
                android:id="@+id/hero_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:shadowColor="#000000"
                android:shadowDx="2"
                android:shadowDy="2"
                android:shadowRadius="4"
                android:textColor="@android:color/white"
                android:textSize="@dimen/hero_title_size"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end"
                tools:text="Good Fortune" />

            <TextView
                android:id="@+id/hero_metadata"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:textColor="#F0FFFFFF"
                android:textSize="@dimen/hero_metadata_size"
                android:textStyle="bold"
                tools:text="98% Match • 2025 • PG-13 • 1h 48m" />

            <!-- Genre Tags -->
            <TextView
                android:id="@+id/hero_genre_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:textColor="@android:color/white"
                android:textSize="@dimen/hero_genre_size"
                android:ellipsize="end"
                android:maxLines="1"
                tools:text="Science Fiction • Adventure • Action" />

            <!-- Overview -->
            <TextView
                android:id="@+id/hero_overview"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:ellipsize="end"
                android:maxLines="4"
                android:textColor="@android:color/white"
                android:textSize="@dimen/hero_overview_size"
                android:lineSpacingMultiplier="1.1"
                tools:text="A well-meaning but rather inept angel named Gabriel meddles in the lives of a struggling gig worker and a wealthy capitalist." />

            <!-- Cast -->
            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp">

                <TextView
                    android:id="@+id/hero_cast"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:textColor="#CCCCCC"
                    android:textSize="@dimen/hero_cast_size"
                    tools:text="Starring: Cailee Spaeny, David Jonsson, Archie Renaux, Isabela Merced" />

                <View
                    android:layout_width="72dp"
                    android:layout_height="match_parent"
                    android:layout_gravity="end"
                    android:background="@drawable/hero_cast_fade" />
            </FrameLayout>
        </LinearLayout>

        <!-- Content Rows -->
        <androidx.leanback.widget.VerticalGridView
            android:id="@+id/content_rows"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1.2"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:clipToPadding="false"
            android:clipChildren="false"
            android:paddingStart="0dp"
            android:paddingTop="8dp"
            android:paddingBottom="12dp" />
    </LinearLayout>

    <!-- Loading Indicator -->
    <ProgressBar
        android:id="@+id/loading_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        tools:visibility="visible" />
</FrameLayout>
