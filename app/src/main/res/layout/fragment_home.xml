<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- Fullscreen Backdrop -->
    <ImageView
        android:id="@+id/hero_backdrop"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop" />

    <!-- Gradient Overlay -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/fullscreen_gradient" />

    <!-- Navigation Sidebar -->
    <LinearLayout
        android:id="@+id/navigation_sidebar"
        android:layout_width="50dp"
        android:layout_height="match_parent"
        android:background="#77000000"
        android:orientation="vertical"
        android:paddingTop="40dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/nav_search"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            app:icon="@drawable/ic_search"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:iconSize="28dp"
            app:iconTint="@color/nav_icon_color" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/nav_home"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            app:icon="@drawable/ic_home"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:iconSize="28dp"
            app:iconTint="@color/nav_icon_color" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/nav_movies"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            app:icon="@drawable/ic_movie"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:iconSize="28dp"
            app:iconTint="@color/nav_icon_color" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/nav_tv_shows"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            app:icon="@drawable/ic_tv"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:iconSize="28dp"
            app:iconTint="@color/nav_icon_color" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/nav_settings"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            app:icon="@drawable/ic_settings"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:iconSize="28dp"
            app:iconTint="@color/nav_icon_color" />
    </LinearLayout>

    <!-- Main Content Area (Overlaying) -->
    <LinearLayout
        android:id="@+id/home_content_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginStart="18dp"
        android:layout_marginTop="0dp"
        android:orientation="vertical"
        android:clipChildren="false"
        android:weightSum="2">

        <!-- Hero Info Section -->
        <LinearLayout
            android:id="@+id/hero_info_container"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:orientation="vertical"
            android:paddingStart="44dp"
            android:paddingEnd="22dp"
            android:layout_marginStart="8dp"
            android:paddingBottom="32dp">

            <ImageView
                android:id="@+id/hero_logo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:layout_marginBottom="8dp"
                android:adjustViewBounds="true"
                android:maxHeight="420dp"
                android:minWidth="820dp"
                android:visibility="gone"
                android:contentDescription="@string/hero_logo_content_description"
                tools:visibility="visible"
                tools:src="@drawable/ic_movie" />

            <!-- Title -->
            <TextView
                android:id="@+id/hero_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:shadowColor="#000000"
                android:shadowDx="2"
                android:shadowDy="2"
                android:shadowRadius="4"
                android:textColor="@android:color/white"
                android:textSize="32sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end"
                tools:text="Good Fortune" />

            <RatingBar
                android:id="@+id/hero_rating_bar"
                style="@style/Widget.AppCompat.RatingBar.Small"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="3dp"
                android:layout_marginBottom="3dp"
                android:isIndicator="true"
                android:numStars="5"
                android:progressTint="#FFFFFF"
                android:progressBackgroundTint="#CCFFFFFF"
                android:secondaryProgressTint="#80FFFFFF"
                android:stepSize="0.1"
                android:visibility="gone"
                tools:visibility="visible"
                tools:rating="4.0" />

            <!-- Meta Info -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/hero_year"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="0dp"
                    android:textColor="@android:color/white"
                    android:textSize="14sp"
                    tools:text="2025" />

                <TextView
                    android:id="@+id/hero_content_rating"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="12dp"
                    android:background="@drawable/content_rating_bg"
                    android:paddingStart="5dp"
                    android:paddingTop="2dp"
                    android:paddingEnd="5dp"
                    android:paddingBottom="2dp"
                    android:textColor="@android:color/white"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:visibility="gone"
                    tools:text="R" />

                <TextView
                    android:id="@+id/hero_runtime"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="12dp"
                    android:textColor="@android:color/white"
                    android:textSize="14sp"
                    tools:text="98m" />
            </LinearLayout>

            <!-- Genre Pills Container -->
            <HorizontalScrollView
                android:layout_width="600dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:scrollbars="none">

                <LinearLayout
                    android:id="@+id/hero_genre_container"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal" />
            </HorizontalScrollView>

            <!-- Overview -->
            <TextView
                android:id="@+id/hero_overview"
                android:layout_width="600dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:ellipsize="end"
                android:maxLines="4"
                android:textColor="@android:color/white"
                android:textSize="14sp"
                android:lineSpacingMultiplier="1.1"
                tools:text="A well-meaning but rather inept angel named Gabriel meddles in the lives of a struggling gig worker and a wealthy capitalist." />

            <!-- Cast -->
            <TextView
                android:id="@+id/hero_cast"
                android:layout_width="600dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="#CCCCCC"
                android:textSize="12sp"
                tools:text="Cailee Spaeny, David Jonsson, Archie Renaux, Isabela Merced" />
        </LinearLayout>

        <!-- Content Rows -->
        <androidx.leanback.widget.VerticalGridView
            android:id="@+id/content_rows"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:clipChildren="false"
            android:paddingStart="32dp"
            android:paddingTop="0dp"
            android:paddingBottom="60dp" />
    </LinearLayout>

    <!-- Coming Soon Placeholder -->
    <LinearLayout
        android:id="@+id/coming_soon_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginStart="18dp"
        android:layout_marginTop="0dp"
        android:layout_marginEnd="48dp"
        android:background="#CC000000"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone">

        <TextView
            android:id="@+id/coming_soon_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Coming soon"
            android:textColor="@android:color/white"
            android:textSize="28sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Loading Indicator -->
    <ProgressBar
        android:id="@+id/loading_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        tools:visibility="visible" />
</FrameLayout>
